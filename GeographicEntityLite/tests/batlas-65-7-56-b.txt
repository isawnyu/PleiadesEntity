
    >>> folder = self.folder
        
    >>> import os
     >>> source_dir = os.path.sep.join([os.environ['SOFTWARE_HOME'], 'Products', 'GeographicEntityLite', 'tests', 'data', 'batlas-65-7-56.xml'])
    >>> from Products.GeographicEntityLite.Extensions.batching import load_entity        
    >>> load_entity(folder, source_dir)
    >>> enID = 'batlas-65-7-56'
    >>> en = getattr(folder, enID)
    >>> en.getIdentifier()
    'batlas-65-7-56'
    
    >>> finalTitle = en.Title()
     
    >>> soughtFinalTitle = u'Sinethandos/Siniandos'
    >>> soughtFinalTitle_utf8 = soughtFinalTitle.encode('utf8')
    >>> finalTitle == soughtFinalTitle_utf8
    True
    >>> finalTitle = en.Title()
    >>> finalTitle == soughtFinalTitle_utf8
    True

    
    >>> sourcelist = ['Roman', 'Late Antique']
    >>> results = en.getTimePeriods()
    
    >>> results[0] == 'Roman'
    True
    
    >>> results[1] == 'Late Antique'
    True
    
    >>> en.getGeoEntityType()
    'unlocated'
    
    >>> sourcelist = [u'BAtlas 65 unlocated Sinethandos/Siniandos', u'RE', u'TIB Phrygien 383']
    >>> resultlist = en.getSecondaryReferences()
    
    >>> resultlist[0] == sourcelist[0].encode('utf8')
    True
    
    >>> resultlist[1] == sourcelist[1].encode('utf8')
    True
    
    >>> resultlist[2] == sourcelist[2].encode('utf8')
    True
    
    >>> nameID = 'batlas-65-7-56-n1'
    >>> en_name = getattr(en, nameID)
    >>> en_name.getIdentifier()
    'batlas-65-7-56-n1'
        
    >>> en_name.Title()
    'Sinethandos'
    
    >>> en_name.getGeoNameType()
    'geographic'
    
    >>> nameID = 'batlas-65-7-56-n2'
    >>> en_name = getattr(en, nameID)
    >>> en_name.getIdentifier()
    'batlas-65-7-56-n2'
        
    >>> en_name.Title()
    'Siniandos'
    
    >>> en_name.getGeoNameType()
    'geographic'
    
    >>> soughtDescription = u'An ancient geographic entity that cannot now be located with certainty, attested during the Roman and Late Antique periods. It was known in antiquity by the names: Sinethandos and Siniandos.'
    >>> gotDescription = en.Description()
    >>> soughtDescription_utf8 = soughtDescription.encode('utf8')
    >>> soughtDescription_utf8 == gotDescription
    True
    